name: "bigquery_auditlog"
version: "0.0.2"

source-paths: ["models"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_modules"

models:
  bigquery_auditlog:
    materialized: view
